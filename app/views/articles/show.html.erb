<div class="container mt-4">
  <h1><%= @article.title %></h1> <!-タイトルの表示->
  <p><%= simple_format(@article.body) %></p> <!--データベースのbodyカラムから本文テキストを取得-->

  <div class="mt-4 d-flex gap-2">
    <%= link_to "Edit", edit_article_path(@article), class: "btn btn-primary" %> <!--edit_article_path(@article)リンク先のURL/クリックでGET /articles/5/edit → 編集画面表示-->
    
    <%= button_to "Delete", @article, 
        method: :delete, #HTTPメソッドの指定、@articleからパスを自動生成("/articles/5")
        data: { confirm: "Are you sure?" },
        class: "btn btn-danger" %>
    
    <%= link_to "Back", articles_path, class: "btn btn-secondary" %> <!--articles_pathがリンク先URL/クリックでGET /articles → 一覧画面表示-->
  </div>
</div>