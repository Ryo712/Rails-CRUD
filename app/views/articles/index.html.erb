<h1>Articles</h1>
<%= link_to"New Article", neew_article_path, "btn btn-primary" %>

<% @article.each do |article| %>
    <div>
        <h3><%= link_to article.title, article %></h3>
<p><%= truncate(article.body, length: 100) %</p>
    </div>

<% end %>